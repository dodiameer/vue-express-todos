{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "vue-express-todos",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "dev": "lerna run dev --stream",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "docker:build": "docker image build -t vueexpresstodos:latest -f ./docker/Dockerfile ."
  }
}
