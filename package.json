{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "vue-express-todos",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "dev": "lerna run dev --stream"
  }
}
